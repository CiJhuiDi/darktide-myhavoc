# myhavoc - 浩劫任务发送 Mod 开发文档

## 概述

- 功能:游戏聊天栏输入 `/myhavoc`,把当前浩劫(Havoc)任务(层数 + 地图 + 词条)发到聊天,展示给队友
- 版本:1.0.0(2026-08-17)
- 仓库:https://github.com/CiJhuiDi/darktide-myhavoc
- 发布:GitHub Release v1.0.0(zip 在 `release\` 或 Releases 页)

## 文件结构

```
myhavoc/
├── myhavoc.mod                          ← DMF 入口清单
├── release\myhavoc_1.0.0.zip            ← 发布包(不进 git,*.zip 已忽略)
└── scripts\mods\myhavoc\
    ├── myhavoc.lua                      ← 主逻辑(命令/数据/发送)
    ├── myhavoc_data.lua                 ← mod 元数据(名称/描述/可开关)
    └── myhavoc_localization.lua         ← 多语言(en + zh-cn)
```

## 核心实现

### 数据来源(浩劫任务)

```lua
Managers.data_service.havoc:current_order()  -- 返回 Promise
-- order.rank            层数
-- order.blueprint.map   地图 id(mission_templates 的 key)
-- order.blueprint.flags 词条表,形如 "havoc-circ-<circumstance_id>"
```

词条显示名:`circumstance_templates[<id>].ui.display_name` → `Managers.localization:localize(key)`
(游戏有官方中文,中文客户端自动出中文;settings 数据在游戏 bundle,运行时 `pcall(require)`)

### 发送消息

```lua
Managers.chat:send_channel_message(channel_handle, text)
-- 频道从 Managers.chat:sessions() 遍历,channel.tag 是字符串("PARTY"/"MISSION"/"HUB")
-- 优先级:队伍 → 任务 → 枢纽
```

### DMF 命令

```lua
mod:command("myhavoc", mod:localize("command_description"), send_my_havoc)
-- 玩家聊天栏输入 /myhavoc 触发;DMF 会拦截命令原文,不会把 "/myhavoc" 发出去
```

## 踩坑/要点

- 本地化语言代码是 `zh-cn`(DMF 按 `Application.user_setting("language_id")` 匹配,缺省回落 en)
- 词条 flags 的键或值都可能是字符串,解析两种都要兼容(`type(k)=="string" and k or type(v)=="string" and v`)
- 所有外部调用(promise/服务/发送)都套 pcall,防止 mod 错误崩游戏
- 浩劫服务在枢纽站可用;没接任务时 current_order 返回无 blueprint,提示"当前没有浩劫任务"

## 参考

- 数据/渲染思路:Wobin 的 Havoc Auspex / Havoc Auspex Transmitter(源码归档在 `..\99-临时文件\havoc_auspex_ref\`)
- 开发规范:`..\01-开发规范\Darktide-Mod开发规范.md`
- 游戏源码:`..\02-游戏源码\Darktide-Source-Code`(havoc_service.lua、constant_element_chat.lua)
